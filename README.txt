A Pen created at CodePen.io. You can find this one at https://codepen.io/gabemartin/pen/Lbtsy.

 Creates a random pattern based on 3 colors. The JS genrates just enough squares to fill the browser window based on the width/height of the window (I made it to use as a background image). It renders pretty fast, but resizing the window is a little funky. 